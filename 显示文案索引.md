# 显示文案索引（修改对照表）

本文件用于帮助你定位**每个用户可见的提示文案**在哪个文件的哪个位置，方便自行改成更人性化的说法。

> ⚠️ **重要前提：修改`.ps1`的中文文案后，请务必保持 `UTF-8 带 BOM` 编码**。
> PowerShell 5.1 缺 BOM 时会按 ANSI(GBK) 解码，中文会出现乱码并导致脚本解析报错。
> 可用记事本“另存为”，编码选 **UTF-8 with BOM（UTF-8 with signature）**。

四个脚本的分工：

- **DSH-Manager.ps1**：主菜单（免双击入口 / 命令入口 `DSH Manager.bat`）
- **install.ps1**：安装/修复（npx 下载、写入文件、创建快捷方式）
- **DSH-Launcher.ps1**：启动（启动服务器、等待端口就绪、打开浏览器）
- **stop.ps1**：停止服务器
- **uninstall.ps1**：卸载

`$AppName`、`$Package`、`$InstallDir`、`$Port`、`$TimeoutS` 等是脚本内的变量，运行时会被替换成真实值（如 `DeepSeek Harness`、`@deepseek-ai/dsh`、`3080`、`600`）。

---

## 一、DSH-Manager.ps1（主菜单）

| 显示内容（原文） | 位置 | 说明 |
|---|---|---|
| `用法: DSH Manager.bat [命令] [参数]` | 第 68 行 | 命令行帮助第一行 |
| `（无参数） 打开交互菜单` `start ...` `stop ...` `install ...` `uninstall ...` | 第 70-74 行 | 命令行用法各条说明 |
| `未检测到 DeepSeek Harness 安装，无法启动。` | 第 87 行 | start 且未安装时 |
| `请先执行安装：DSH Manager.bat install（或菜单选择 [3] 安装/修复）。` | 第 88 行 | start 且未安装时续行 |
| `未知命令: $cmd` | 第 108 行 | 命令行收到未知命令 |
| `====` `$AppName 管理工具` `状态：$(Get-StatusLine)` `====` | 第 120-123 行 | 交互菜单标题与状态栏 |
| `[1] 启动` `[2] 停止` `[3] 安装/修复` `[4] 卸载` `[5] 退出` | 第 124-128 行 | 菜单选项 |
| `请选择` | 第 130 行 | `Read-Host` 菜单等待输入 |
| `未检测到 DeepSeek Harness 安装，无法启动。` + `请先选择 [3] 安装/修复...` | 第 134-135 行 | 菜单选启动但未安装 |
| `无效选择，请输入 1-5。` | 第 144 行 | 菜单输入非法 |
| `（按回车立即返回菜单，或等待 3 秒自动返回…）` | 第 147 行 | 操作结束后的返回提示 |

## 二、install.ps1（安装/修复）

| 显示内容（原文） | 位置 | 说明 |
|---|---|---|
| `未检测到 Node.js。...` | 第 49 行 | 缺 Node.js 时的错误弹窗 |
| `未检测到 npm。...` | 第 53 行 | 缺 npm 时的错误弹窗 |
| `Node.js: ... npm: ...` | 第 56 行 | 检测到的版本信息 |
| **`Downloading $Package ...`** | **第 61 行** | **下载开头的一行提示（本次已改）** |
| **`Write-Host -NoNewline '.'`** | **第 87-90 行** | **下载等待的进度点：每 10 秒打一个点（本次已改）** |
| `DeepSeek Harness 安装超时（等待 600 秒未完成）...` | 第 113 行 | 下载超过 10 分钟 |
| `DeepSeek Harness 安装失败（npx 退出码 ...）...` | 第 117 行 | npx 下载失败 |
| `DeepSeek Harness 安装失败。未能在 npx 缓存中定位...` | 第 125 行 | 缓存未找到包 |
| `安装完成（npx 缓存：...）` | 第 128 行 | 下载完成 |
| `已从备份恢复: $f` `已写入: $f` `已从备份恢复（源缺失）: $f` | 第 148/156/161 行 | 修复模式写文件时的日志 |
| `文件与修复备份已就绪: $InstallDir` | 第 164 行 | 文件写入完成 |
| `桌面快捷方式已创建: $lnkPath` | 第 177 行 | 快捷方式创建成功 |
| `安装完成！启动命令：npx @deepseek-ai/dsh web` | 第 180 行 | 命令行版完成提示 |
| `安装完成！桌面已创建...`（弹窗） | 第 181 行 | 完成弹窗 |
| `是否立即启动 DeepSeek Harness？(Y/N，默认 N)` | 第 186 行 | `Read-Host` 询问是否启动 |
| `正在启动...` | 第 191 行 | 选 Y 后的连接提示 |
| `安装失败：...`（弹窗） | 第 196 行 | 未捕获异常 |

## 三、DSH-Launcher.ps1（启动）

| 显示内容（原文） | 位置 | 说明 |
|---|---|---|
| `未检测到 Node.js。...` | 第 77 行 | 缺 Node.js 错误 |
| `GUI 已在运行：$GuiUrl` | 第 83 行 | 已检测到运行的服务器 |
| `未检测到运行中的 GUI，正在启动 DSH Web 服务器...` | 第 85 行 | 开始启动 |
| `使用命令：npx -y @deepseek-ai/dsh web（自动更新到最新版）` | 第 97 行 | npx 模式 |
| `使用命令：$dsh web（全局版本，不自动更新）` | 第 100 行 | 全局模式 |
| `未找到全局 dsh，改用：npx ...` | 第 104 行 | 回退 npx |
| `未找到 npx 或 dsh。...` | 第 106 行 | 两者皆无 |
| **`正在启动服务器，请稍候…（首次需联网下载/更新 dsh 包，请勿关闭本窗口）`** | **第 115 行** | **启动提示（本次已改中文）** |
| `启动 dsh web 失败：...`（弹窗） | 第 119 行 | 启动进程抛异常 |
| `启动命令已退出，服务器未能启动。...`（弹窗） | 第 132 行 | 进程提前退出 |
| `服务器在 ... 秒内未能启动...`（弹窗） | 第 136 行 | 等待超时 |
| **`Write-Host -NoNewline '.'`** | **第 139-141 行** | **等待端口就绪的进度点（每 10 秒一个，本次已改）** |
| `服务器已就绪（约 ...s）。` | 第 144 行 | 就绪提示 |
| `正在打开浏览器：$GuiUrl` | 第 147 行 | 打开浏览器 |
| `打开浏览器失败：...`（弹窗） | 第 151 行 | 打开失败 |
| `完成。` | 第 155 行 | 启动流程收尾 |

## 四、stop.ps1（停止）

| 显示内容（原文） | 位置 |
|---|---|
| `已停止服务器进程 (PID ...)` | 第 42 行 |
| `端口 $Port 被非 DSH 进程占用 ... 已跳过。` | 第 45 行 |
| `端口 $Port 上的进程 (PID ...) 无法读取信息，跳过...` | 第 47 行 |
| `服务器已停止。` | 第 52 行 |
| `未发现运行中的 DSH Web 服务器（端口 $Port）。` | 第 54 行 |

## 五、uninstall.ps1（卸载）

| 显示内容（原文） | 位置 |
|---|---|
| `开始卸载...` | 第 41 行 |
| `将停止服务器、卸载 dsh（清理 npx 缓存与旧版全局包）并删除全部文件。确定继续吗？(Y/N)`（`Read-Host`） | 第 46 行 |
| `已取消卸载。` | 第 51 行 |
| `已终止 dsh 相关进程 (PID ...)` | 第 97 行 |
| `已停止服务器进程 (PID ...)` | 第 107 行 |
| `端口 3080 被非 DSH 进程占用 ... 已跳过。` | 第 109 行 |
| `端口 3080 上的进程 ... 无法读取信息，跳过...` | 第 111 行 |
| `卸载 npm 全局包（若存在）: npm uninstall -g ...` | 第 117 行 |
| `警告：npm 卸载返回非零退出码（未全局安装，可忽略）。` | 第 120 行 |
| `未检测到 npm，跳过 npm 全局包卸载。` | 第 123 行 |
| `已删除桌面快捷方式: ...` | 第 131 行 |
| `桌面快捷方式不存在，跳过` | 第 133 行 |
| `警告：部分文件未能删除（可能被占用）...` | 第 140 行 |
| `已删除安装目录: $InstallDir` | 第 142 行 |
| `安装目录不存在，跳过` | 第 145 行 |
| `警告：npx 缓存条目 ... 仍被占用未能删除...` | 第 156 行 |
| `已清理 npx 缓存条目: ...` | 第 158 行 |
| `是否同时删除用户数据？位于: ...`（及其 3 行说明：删除/保留/推荐） | 第 168-172 行 |
| `确定删除用户数据吗？(y/N，默认 N)`（`Read-Host`） | 第 174 行 |
| `用户数据目录不存在，无需删除: ...` | 第 182 行 |
| `已跳过删除：DSH_HOME 指向的不是目录...` | 第 184 行 |
| `已跳过删除：DSH_HOME 路径异常...` | 第 195 行 |
| `警告：部分用户数据未能删除（可能被占用）...` | 第 199 行 |
| `已删除用户数据目录: ...` | 第 201 行 |
| `已保留用户数据目录: ...` | 第 206 行 |
| `卸载完成。` + 完成弹窗 | 第 210-211 行 |

---

## 本次改动说明（等待提示的新样式）

- **install.ps1**：下载开始了显示一行
  `[$AppName] Downloading $Package ...`（第 61 行），
  之后每 **10 秒**打一个点 `....`（第 87-90 行），不再每隔 30 秒报“已等待 X 秒”。
- **DSH-Launcher.ps1**：启动提示改为一句话
  `正在启动服务器，请稍候…（首次需联网下载/更新 dsh 包，请勿关闭本窗口）`（第 115 行），
  等待端口就绪时每 **10 秒**打一个点（第 139-141 行），就绪后换行提示“服务器已就绪”。

> 想调整“每个点几秒”：把 `% 10`（install 第 88 行、launcher 第 139 行）里的 10 改成你想要的秒数即可。